import React from "react";

const Player = () => {
    return (
        <aside className="text-white bg-purple-400 fixed bottom-0 left-0 right-0 h-16 flex px-4 m-3 rounded-full">
            a
        </aside>
    );
};

export default Player;